export interface ITablesAvalible {
  sixaclock: boolean;
  nineaclock: boolean;
}

export interface ISixDisable {
  sixaclock: boolean;
  isDisabled: boolean;
}

export interface INineDisable {
  nineaclock: boolean;
  isDisabled: boolean;
}
